sort(v.begin(),v.end()) : NlogN
stable_sort(v.being(),v.end()): NlogN   used to preserve releative order of equal value usually when we have mulptiple keys

binary_search(v.being(),v.end(),key): return true/false  LogN    sorted array 

lower_bound(v.being(),v.end(),key): return iterator to the fist element>=key    //must be sorted 
uppoer_bound(v.begin(),v.end(),key): return ierator to the first element>key    
eg. vector<int> v = {1, 2, 2, 2, 4, 5};
int count = upper_bound(v.begin(), v.end(), key)  -  lower_bound(v.begin(), v.end(), key);
lower_bound finds where a value can start,
upper_bound finds where a value must end.
